import React, { Component } from "react";
import { compose } from "redux";

class ChillerSettings extends Component {
  render() {
    return <h5>Chiller settings here</h5>;
  }
}

export default compose()(ChillerSettings);
